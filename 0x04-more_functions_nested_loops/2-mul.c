#include "main.h"

/**
 * mul - multiplies
 * two integers
 * @a: the first integer
 * @b: the second integer
 * Return: always 1
 */
int mul(int a, int b)
{
	int c;

	for (c = (a * b);)
	{
		return (c);
	}
}
