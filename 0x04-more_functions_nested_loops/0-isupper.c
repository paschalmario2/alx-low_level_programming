#include "main.h"

/**
 * _isupper - checks if paremeter is an uppercase letter
 * @c: input character
 * Return: 1 if an uppercase letter, 0 if it is other case
 */
int _isupper(int c)
	if (c >= 65 && c <= 90)
{
	return (1);
}
else
{
	return (0);
}
}
